<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0"><!-- TODO change it to 1 when finish setup all rules -->

        <record id="major_account_multi_company_rule" model="ir.rule">
            <field name="name">Major Accounts multi company rule</field>
            <field name="model_id" ref="model_account_major"/>
            <field eval="True" name="global"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', '=', user.company_id.id)]</field>
        </record>
    </data>
</odoo>