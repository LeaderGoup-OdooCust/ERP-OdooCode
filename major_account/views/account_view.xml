<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Adding Major Account field int the account form view -->
        <record id="view_account_major_account_form" model="ir.ui.view">
            <field name="name">Account Details</field>
            <field name="model">account.account</field>
            <field name="inherit_id" ref="account.view_account_form"/>
            <field name="arch" type="xml">
                <field name="tag_ids" position="after">
                    <field name="major_account_id" required="1"
                           context="{'default_is_leaf':1}"
                           options = "{'no_create':True}"/>
                </field>
                <field name="group_id" position="attributes">
                    <attribute name="invisible">1</attribute>
                </field>
            </field>
        </record>
        <record id="view_account_major_account_list" model="ir.ui.view">
            <field name="name">account.account.list</field>
            <field name="model">account.account</field>
            <field name="inherit_id" ref="account.view_account_list"/>
            <field name="arch" type="xml">
                <field name="company_id" position="after">
                    <field name="major_account_id" invisible="1"/>
                </field>
                <tree position="attributes">
                    <attribute name="colors">red:not major_account_id</attribute>
                    <attribute name="fonts">bold:not major_account_id</attribute>
                </tree>
            </field>
        </record>
    </data>

</odoo>
